# backup source
SOURCE_PATH=/

# the script mounts before rsync and unmounts after rsync
MOUNT_PATH=/mnt/foo

# backup target
TARGET_PATH=/tmp/test

# skip mount/unmount if set 1
SKIP_MOUNT=1

# use compression
COMPRESSION=1

# do rsync excluding these
EXCLUDES="/dev /lost+found /media /mnt /proc /selinux /srv /sys cache/ Cache/ tmp/"

# purge some oldest backup sets if disk usage % is greater than this
THRESHOLD=95

# script log
LOG=/var/log/backup.log
